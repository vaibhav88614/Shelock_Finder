# Runtime backend deps
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.17

# DB
SQLAlchemy==2.0.36
alembic==1.14.0

# HTTP / scraping
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0
playwright==1.48.0
# TLS/JA3 browser impersonation for the jsonapi adapter's optional "impersonate"
# mode, used to reach APIs behind bot walls that reject the default httpx client.
curl_cffi==0.16.3

# Utilities
loguru==0.7.2
python-dateutil==2.9.0.post0
typer==0.13.1
rich==13.9.4
# PDF text extraction for resume uploads. Pure-python (no native deps) so it
# works on the same runtime as the rest of the backend without a compile step.
pypdf==5.1.0

# Tests
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1
