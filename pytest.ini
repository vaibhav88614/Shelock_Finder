[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
# DeprecationWarnings surfaced by default after the utcnow_naive migration
# (Phase 2.1). Add targeted ignores here if a third-party dep starts
# emitting noise we can't act on.
filterwarnings =
    default::DeprecationWarning
